# Production Environment Variables
# NOTE: VITE_API_URL is NOT used in production
# The Netlify proxy in netlify.toml handles all /api/* requests
# and routes them to https://ldss-backend-5tl3.onrender.com/api/*

# Leaving this empty or commented out to ensure relative paths are used
# VITE_API_URL=
